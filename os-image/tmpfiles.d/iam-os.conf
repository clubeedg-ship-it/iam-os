# Owns /run/iam-os/ so the lidar frame sink, calibration pointer, and status
# file the services write all live in a single, predictable place. systemd
# creates this on boot before any IAM-OS unit starts.

d /run/iam-os 0755 iam-os iam-os -
