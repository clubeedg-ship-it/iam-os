/**
 * Test view — placeholder.
 *
 * The real live-touch canvas lands in the next commit.
 */
export function renderTest(container, _context) {
  container.innerHTML = `
    <h1 class="view-title">Test</h1>
    <p class="view-help">Watch live touches to confirm tracking is healthy.</p>
    <section class="panel">Coming in the next commit.</section>
  `;
}
