/**
 * Games view — placeholder.
 *
 * The real game catalogue + launch lands in the next commit.
 */
export function renderGames(container, _context) {
  container.innerHTML = `
    <h1 class="view-title">Games</h1>
    <p class="view-help">Pick a game to hand the screen over to it.</p>
    <section class="panel">Coming in the next commit.</section>
  `;
}
