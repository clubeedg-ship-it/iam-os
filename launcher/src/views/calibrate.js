/**
 * Calibrate view — placeholder.
 *
 * The real four-corner capture flow lands in the next commit.
 */
export function renderCalibrate(container, _context) {
  container.innerHTML = `
    <h1 class="view-title">Calibrate</h1>
    <p class="view-help">Map the four corners of the projection surface.</p>
    <section class="panel">Coming in the next commit.</section>
  `;
}
